- [x] The two completed plans at the repository root are removed, and `a11y-fix-plan.md` now covers only the open drizzle-cube item.

## What changed

- **Deleted `login-hang-plan.md`.** Fully implemented in `44a4173` (AuthFailure card, `/login` error branch, secure-context boot gate, boot-chain `.catch`, the hang-invariant test). Referenced nowhere.
- **Deleted `number-format-plan.md`.** Fully implemented in `595d79f` (`lib/number-format.ts`, `ui-ext/number-input.tsx`, grid formatting, `precision`). Referenced nowhere.
- **`a11y-fix-plan.md` cut to drizzle-cube only.** Every code fix it listed landed on 2026-09-06; what remains is vendor markup on the `/nwind` dashboard. The file now states:
  - WCAG 1.4.3: fixed locally in `theme-a11y.css`.
  - WCAG 1.3.1: the "No Portlets" `<h1>` to `<h3>` jump, still open. Installed 0.5.8, npm latest 0.9.3.
  - Three ways to close it: upgrade, report upstream, or our own empty state.
  - The drafted upstream issue text, and when the file can be deleted.
  
  The complete earlier version stays in history at `69df1ba`.
- **Pointers into removed sections updated:**
  - `ACCESSIBILITY.md:62` names the file without the old `§ 4.4`.
  - `scripts/a11y-audit/probes.mjs:338` now says the page-behind defect is fixed by `components/a11y/ModalInert.tsx`, instead of pointing to the old `§ 4.3h`.
- **Left as is:** `a11y-audit-review.md`, a dated review, still quotes sections of the old plan as they were at review time.

## Verification

- `pnpm check`: lint 0 errors (18 React Compiler warnings, as on `main`). Tests: 91 files, 1042 passed, 4 skipped. The skips are the dark-only `describe.runIf` block in `tokenContrast.test.ts`, which is skipped on the light-theme pass.
- `pnpm build`: green.

## Preview

https://docsplancl-20260919183350-semantius-app.ma532.workers.dev

## Screenshots

![Preview home page, signed in, Modules page with sidebar](https://raw.githubusercontent.com/semantius/semantius-app/docs/plan-cleanup/screenshots/20260919183533-plan-cleanup-home.png)

`screenshots/20260919183533-plan-cleanup-home.png` shows the Cloudflare preview at 1440x900, signed in through `#jwt`, on the Modules page. Confidence 90% that the build deploys and boots. This PR changes only documents and one audit-script comment, so there is no UI change to show.

## CONTEXT-MEMORY.md

No update needed. The change deletes and trims plan documents. It adds no platform constraint or convention a future session would otherwise get wrong.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
