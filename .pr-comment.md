- [x] Move the light/dark mode selector from the top-right header into the user menu, keeping System as the default

## Preview

Could not deploy a Cloudflare preview from this Cursor Cloud sandbox: `DOTENV_PRIVATE_KEY` is not injected, so the committed `.env` cannot decrypt `CLOUDFLARE_API_TOKEN`. GitHub Actions Checks on this branch passed (lint + unit tests): https://github.com/semantius/semantius-app/actions/runs/35522692758

## Screenshots

Could not capture Cloudflare preview screenshots for the same reason. The user-visible states covered by tests (without screenshot) are:

- Header no longer has the theme toggle
- Account menu offers a Theme submenu
- System is checked when nothing has been chosen (`semantius-ui-theme` stays unset)
- Choosing Light or Dark persists and paints that theme
- Choosing System writes `system` and follows the OS again

## CONTEXT-MEMORY.md

No update needed — the theme still uses next-themes with `defaultTheme="system"` and `storageKey="semantius-ui-theme"`; only the control moved from the header into the existing account-menu pattern.
