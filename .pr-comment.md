- [x] Scaffold custom table chart from built-in `table` type using `drizzle-cube charts init --from table`
- [x] Create `CustomTableChart.tsx` based on DataTable source, using only public `drizzle-cube/client` exports
- [x] Create `CustomTableChart.config.ts` with drop zones and display options
- [x] Register as `type: 'table'` in `src/charts/index.ts` to override the built-in
- [x] Pass `customCharts` to `CubeProvider` in `_app.$moduleId.index.tsx`
- [x] Build passes, CodeQL clean
- [x] Deployed and verified on Cloudflare preview
- [x] Updated `CONTEXT-MEMORY.md` with chart plugin architecture and `.env` restore procedure

## Preview

https://overridebu-20260411144340-semantius-web.ma532.workers.dev

## Screenshots

![Cloudflare preview deployed and serving HTTP 200](https://raw.githubusercontent.com/semantius/semantius-app/copilot/override-built-in-charts/screenshots/20260411144503-preview-deployed.png)

## CONTEXT-MEMORY.md

Updated — added "Secrets & Deployment" section explaining that `.env` was deleted in commit `114aed6`, how to restore it from git history (`git show 9ef17da:.env > .env`), and that `DOTENV_PRIVATE_KEY` is already injected by the sandbox so no manual key configuration is needed. Also added note that the deploy health-check curl / message webhook may fail non-fatally after a successful wrangler upload.
