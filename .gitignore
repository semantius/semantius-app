# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.turbo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.dist
.env.keys
.env.*
!.env.sample
!.env.example

# Docker runtime config holds real values — commit only the .env.example files.
# Bare-name .env needs its own rule: the .env.* pattern above does not match it.
docker/.env
docker-vo/.env

# Workplace version tracking
.workplace-version
